Tags:
- [[Python]]
---
- files with the `.pth` extension, usually inside `site-packages` of the python env
- defines what should be _appended_ to the interpreter's `sys.path` on start up
- can also have executable lines that run on the interpreter's start up
---
## References
- https://docs.python.org/3/library/site.html